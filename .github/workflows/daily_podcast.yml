name: Update Podcasts (Tor + Git Fix)

on:
  schedule:
    - cron: '0 4 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      with:
        fetch-depth: 0

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '16'
    
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: Install Libraries
      run: |
        sudo apt-get install -y ffmpeg
        # Installation explicite de stem pour piloter Tor
        pip install stem
        # CRITIQUE : Toujours installer la version MASTER de yt-dlp pour les fix 403
        pip install https://github.com/yt-dlp/yt-dlp/archive/master.zip
        pip install -r requirements.txt --upgrade

    # --- TOR BELGIQUE ---
    - name: Install and Configure Tor (Belgium Exit)
      run: |
        sudo apt-get install -y tor
        
        # Configuration Tor avec ControlPort pour le renouvellement d'IP
        # On sécurise avec un mot de passe haché ou sans auth pour localhost (ici CookieAuthFile plus simple pour script)
        echo "ControlPort 9051" | sudo tee -a /etc/tor/torrc
        echo "CookieAuthentication 1" | sudo tee -a /etc/tor/torrc
        
        # Configuration Belgique
        echo "ExitNodes {BE}" | sudo tee -a /etc/tor/torrc
        echo "StrictNodes 1" | sudo tee -a /etc/tor/torrc
        
        # Optimisations
        echo "CircuitBuildTimeout 60" | sudo tee -a /etc/tor/torrc
        echo "KeepalivePeriod 60" | sudo tee -a /etc/tor/torrc
        echo "NumEntryGuards 8" | sudo tee -a /etc/tor/torrc
        echo "ClientOnly 1" | sudo tee -a /etc/tor/torrc
        echo "MaxCircuitDirtiness 600" | sudo tee -a /etc/tor/torrc
        
        sudo service tor start
        
        echo "Démarrage de Tor... (Pause 60s)"
        sleep 60
        
        # VERIFICATION
        echo "Test de connexion..."
        curl -s --socks5 127.0.0.1:9050 -L -A "Mozilla/5.0" https://icanhazip.com || echo "Test IP échoué (mais Tor tourne peut-être)"

    - name: Run Script via Tor
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        YOUTUBE_COOKIES: ${{ secrets.YOUTUBE_COOKIES }}
        YOUTUBE_PROXY: "socks5://127.0.0.1:9050"
      run: python podcast_gen.py
      
    - name: Commit and Push Changes
      if: always() 
      run: |
        git config --global user.name 'Bot'
        git config --global user.email 'bot@noreply.github.com'
        git add .
        if git diff --staged --quiet; then
          echo "Rien à commiter."
        else
          git commit -m "Update RSS and Logs"
          git pull --rebase origin main
          git push
        fi
